# Limpieza de archivos sin uso — 6 ago 2026

Análisis de alcanzabilidad desde los entrypoints de Next (`src/pages/**`) siguiendo todas las cadenas de `import` / `require` / `@import`. Todo lo que no era alcanzable se eliminó.

Respaldo comprimido de lo borrado: `vallarta-wknd-backup-limpieza-20260806.tar.gz`. El proyecto no tiene repo git, así que ese archivo es la única vía de recuperación.

## Eliminado de `/src` (35 archivos)

### Componentes huérfanos del proyecto anterior (hotel)

`Calificacion.js`, `ImageSlider.js`, `ImageSlider.module.css`, `WeOffer.js`, `blockbuster.js`, `calendario.js`, `caracteristicas.js`, `client-logos.js`, `descripcion-detallada.js`, `faqs.js`, `imagegrid.js`, `reserva.js`, `reserva_old.js`, `slider.js`, `subtitulo.js`, `titulo.js`

### Formularios y UI sin referencia

`form/form.js`, `form/input.js`, `form/opt-in-form-h.js`, `navigation/menu.js` (carpeta completa), `ui/actionButton.js`, `ui/actionButton2.js`, `ui/verMasButton.js`, `ui/whatsButton.js`

### Servicios y APIs muertos

- `services/mail.js` + `pages/api/send-mail.js` (SendGrid, nadie los llamaba)
- `services/googleSheetDB.js` + `pages/api/save-gsheet.js` (Google Sheets, sin uso)
- `services/makeLeadHandler.js`
- `pages/api/hello.js` (boilerplate de Next)

El formulario activo (`opt-in-form.js`) envía a `info.optInWebhook` (n8n). Si más adelante se quiere correo transaccional o registro en Sheets, hay que restaurar esos archivos del respaldo.

### Estilos

- `styles/globals.css` y `styles/globals.css.map`: artefactos compilados de Sass que quedaron en el repo. Next compila `globals.scss` en build.
- `styles/DatePickerCustom.css`: sin importar.

Se conservan `globals.scss`, `type.scss` y `form.scss`. Los dos últimos entran vía `@import` dentro de `globals.scss`.

### Utils

`size-observer.js`, `use-animation-frame.js`

## Eliminado de `/public` (~2.3 MB)

| Ruta | Motivo |
|---|---|
| `icons/` (24 PNG) | Iconografía de amenidades de hotel, ajena a tours |
| `img-Comentarios/` (6 PNG) | Reseñas viejas; `ReviewList.js` no usa imágenes |
| `imgSlider/whale-watching-safari/` (2.1 MB) | Slug inexistente en `DataAtlas.js` |
| `icons-svg/bebida.svg`, `icons-svg/caja-seguridad.svg` | Sin `key` correspondiente en `includes` |
| `logo.png`, `logo.svg` | El logo se renderiza como SVG inline en `ui/logo.js` |
| `vercel.svg` | Boilerplate de Next |
| 8 archivos `.DS_Store` | Basura de macOS (ya está en `.gitignore`) |

## Qué se conservó a propósito

- `public/imgSlider/{home,madagascar-animas,adelita-yate-privado,vallarta-whale-watching}/`: se leen dinámicamente en build por `utils/galleryImages.js`, no aparecen como literales en el código.
- `public/icons-svg/*.svg` (15 restantes): referenciados dinámicamente vía `` `/icons-svg/${key}.svg` `` con los `key` de `DataAtlas.js`.
- `public/typetura.js`: cargado en `_document.js`.
- `public/landing/badge-*.png`: declarados en `DataAtlas.js` (`badges`). Ojo, `index.js` desestructura `badges` pero nunca lo renderiza. Es un pendiente de implementación, no basura.
- `public/favicon.ico`.

## Pendientes detectados (no tocados)

1. **`badges` sin renderizar** en `src/pages/index.js` línea 57.
2. **Dependencias de `package.json` ahora sin uso**: `@sendgrid/mail`, `googleapis`, `@datepicker-react/styled`, `@splidejs/*`, `@types/swiper`, `flowbite`, `@radix-ui/react-slot`, y `react-calendly` (solo en `scheduler.js`, revisar si esa página sigue viva). Depurarlas bajaría bastante el `node_modules`.
3. **Proyecto sin git**. Vale la pena inicializar repo antes del siguiente cambio grande.

## Verificación

- Re-corrida del análisis de alcanzabilidad: 0 archivos huérfanos restantes en `/src`.
- Chequeo de resolución de importaciones: todas las rutas locales resuelven.
- `next build` no se pudo correr en el entorno de análisis porque `node_modules` está compilado para macOS y falta el binario SWC de Linux. Conviene correr `npm run build` localmente para confirmar.
